# Parameters

## Required

| name                          | type                         | detail                                                                       |
|-------------------------------|------------------------------|------------------------------------------------------------------------------|
| revoicechat.global.sever-mode | `MONO_SERVER`/`MULTI_SERVER` | tell if the app provide a single chat server, or if we can have multi server |

## with default falue

| name                             | default value | detail                   |
|----------------------------------|---------------|--------------------------|
| revoicechat.cors.allowed-origins | *             | origins allowed for CORS |
